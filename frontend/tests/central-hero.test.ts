import test from 'node:test';import assert from 'node:assert/strict';import{readFileSync}from'node:fs';test('hero centers headline and actions while retaining next scroll scene',()=>{const s=readFileSync(new URL('../app/page.tsx',import.meta.url),'utf8');assert.match(s,/data-central-hero/);assert.match(s,/text-center/);assert.match(s,/justify-center/);assert.match(s,/<ProductBanner/)});
